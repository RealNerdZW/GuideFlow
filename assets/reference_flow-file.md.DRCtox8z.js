import{_ as t,o as i,c as s,ag as a}from"./chunks/framework.CVaJrGn7.js";const g=JSON.parse('{"title":"The .flow.json format","description":"The .flow.json file format, the @guideflow/core/authoring validator with its full rule table, and the @guideflow/core/selector builder — every export, signature and severity.","frontmatter":{"description":"The .flow.json file format, the @guideflow/core/authoring validator with its full rule table, and the @guideflow/core/selector builder — every export, signature and severity.","keywords":"flow.json format, GuideFlow flow file, validateFlow, flow validation, buildSelector, CSS selector generation, guideflow validate","head":[["link",{"rel":"canonical","href":"https://realnerdzw.github.io/GuideFlow/reference/flow-file"}],["meta",{"property":"og:url","content":"https://realnerdzw.github.io/GuideFlow/reference/flow-file"}],["meta",{"property":"og:title","content":"The .flow.json format — GuideFlow.js"}],["meta",{"property":"og:description","content":"The .flow.json file format, the @guideflow/core/authoring validator with its full rule table, and the @guideflow/core/selector builder — every export, signature and severity."}],["meta",{"name":"twitter:title","content":"The .flow.json format — GuideFlow.js"}],["meta",{"name":"twitter:description","content":"The .flow.json file format, the @guideflow/core/authoring validator with its full rule table, and the @guideflow/core/selector builder — every export, signature and severity."}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"BreadcrumbList\\",\\"itemListElement\\":[{\\"@type\\":\\"ListItem\\",\\"position\\":1,\\"name\\":\\"Home\\",\\"item\\":\\"https://realnerdzw.github.io/GuideFlow/\\"},{\\"@type\\":\\"ListItem\\",\\"position\\":2,\\"name\\":\\"Reference\\",\\"item\\":\\"https://realnerdzw.github.io/GuideFlow/reference/\\"},{\\"@type\\":\\"ListItem\\",\\"position\\":3,\\"name\\":\\"Flow File\\",\\"item\\":\\"https://realnerdzw.github.io/GuideFlow/reference/flow-file/\\"}]}"]]},"headers":[],"relativePath":"reference/flow-file.md","filePath":"reference/flow-file.md","lastUpdated":1786207781000}'),o={name:"reference/flow-file.md"};function d(n,e,r,l,h,c){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="the-flow-json-format" tabindex="-1">The <code>.flow.json</code> format <a class="header-anchor" href="#the-flow-json-format" aria-label="Permalink to &quot;The \`.flow.json\` format&quot;">​</a></h1><p>A flow is a state machine, and a <code>.flow.json</code> file is that state machine written down. This page is the reference for the file envelope and for the two authoring-time subpaths that read and write it: <a href="#guideflow-core-authoring"><code>@guideflow/core/authoring</code></a> and <a href="#guideflow-core-selector"><code>@guideflow/core/selector</code></a>.</p><p>Both are separate entry points. A running app never validates a flow and never builds a selector — the author does, once, at authoring time or in CI. Neither is imported by <code>src/index.ts</code>, so <code>dist/index.js</code> is unchanged.</p><h2 id="the-envelope" tabindex="-1">The envelope <a class="header-anchor" href="#the-envelope" aria-label="Permalink to &quot;The envelope&quot;">​</a></h2><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gfFlowFile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;flow&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;welcome&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;initial&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;intro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;states&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;intro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;steps&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;greeting&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;content&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;title&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Welcome&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;body&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Let&#39;s take a quick look around.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;target&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;[data-gf-id=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">nav-home</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">]&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;on&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;NEXT&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;done&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;done&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;steps&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;finish&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;content&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;title&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;That&#39;s it&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;final&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1k3jf9&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;meta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;generator&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@guideflow/core 0.1.9&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;createdAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-08-01T09:12:04.118Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;sourceUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://app.example.com/dashboard&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead><tbody><tr><td><code>gfFlowFile</code></td><td><code>1</code></td><td>yes</td><td>Envelope version, <strong>not</strong> a library version. Any other value is the <code>unsupported-envelope</code> error and parsing stops</td></tr><tr><td><code>flow</code></td><td><code>FlowDefinition</code></td><td>yes</td><td>The state machine itself. See <a href="/GuideFlow/api/flow-definition">FlowDefinition</a></td></tr><tr><td><code>meta</code></td><td><code>FlowFileMeta</code></td><td>no</td><td>Provenance. Nothing in the engine reads it</td></tr></tbody></table><h3 id="flowfilemeta" tabindex="-1"><code>FlowFileMeta</code> <a class="header-anchor" href="#flowfilemeta" aria-label="Permalink to &quot;\`FlowFileMeta\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>generator</code></td><td><code>string</code></td><td>What wrote the file</td></tr><tr><td><code>createdAt</code></td><td><code>string</code></td><td>ISO 8601</td></tr><tr><td><code>sourceUrl</code></td><td><code>string</code></td><td>The page the flow was authored against</td></tr></tbody></table><h3 id="there-is-no-schema-key" tabindex="-1">There is no <code>$schema</code> key <a class="header-anchor" href="#there-is-no-schema-key" aria-label="Permalink to &quot;There is no \`$schema\` key&quot;">​</a></h3><p>Deliberately. We host no schema, and a URL that 404s in every editor that follows it would be a lie inside the artifact this format exists to make honest.</p><p><code>validateFlow</code> <strong>is</strong> the schema. It carries about thirty rules, each grounded in a behaviour measured against the real engine, and it ships in the same package as the engine — so it cannot drift from the thing it describes the way a hosted JSON Schema would. Run it in CI with <a href="#in-ci"><code>guideflow validate</code></a>.</p><h3 id="reading-one-back-needs-no-loader" tabindex="-1">Reading one back needs no loader <a class="header-anchor" href="#reading-one-back-needs-no-loader" aria-label="Permalink to &quot;Reading one back needs no loader&quot;">​</a></h3><p>The envelope is plain JSON, so a bundler&#39;s JSON import is the whole story:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> doc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./welcome.flow.json&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gf.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(doc.flow)</span></span></code></pre></div><p>Use <a href="#parseflowfile-input"><code>parseFlowFile</code></a> instead when the input is untrusted — a file a user uploaded, or one fetched at runtime — because it validates as it reads and hands back the issue list rather than a flow the engine will fail on later.</p><h2 id="guideflow-core-authoring" tabindex="-1"><code>@guideflow/core/authoring</code> <a class="header-anchor" href="#guideflow-core-authoring" aria-label="Permalink to &quot;\`@guideflow/core/authoring\`&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># already installed — it is a subpath of core, not a package</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { validateFlow, stringifyFlowFile, parseFlowFile } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@guideflow/core/authoring&#39;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Export</th><th>Signature</th></tr></thead><tbody><tr><td><code>validateFlow</code></td><td><code>(input: unknown, options?: ValidateOptions) =&gt; FlowValidation</code></td></tr><tr><td><code>draftToFlow</code></td><td><code>&lt;TContext&gt;(draft: FlowDraft) =&gt; FlowDefinition&lt;TContext&gt;</code></td></tr><tr><td><code>flowToDraft</code></td><td><code>(flow: FlowDefinition) =&gt; { draft: FlowDraft | null; lossy: FlowIssue[] }</code></td></tr><tr><td><code>explainNotLinear</code></td><td><code>(flow: FlowDefinition) =&gt; string | null</code></td></tr><tr><td><code>stringifyFlowFile</code></td><td><code>(flow: FlowDefinition, meta?: FlowFileMeta) =&gt; string</code></td></tr><tr><td><code>parseFlowFile</code></td><td><code>(input: unknown) =&gt; FlowValidation &amp; { meta: FlowFileMeta | null }</code></td></tr></tbody></table><p>Types: <code>FlowIssue</code>, <code>FlowIssueCode</code>, <code>FlowValidation</code>, <code>ValidateOptions</code>, <code>LinearStep</code>, <code>FlowDraft</code>, <code>FlowFile</code>, <code>FlowFileMeta</code>, <code>Severity</code>.</p><h3 id="validateflow-input-options" tabindex="-1"><code>validateFlow(input, options?)</code> <a class="header-anchor" href="#validateflow-input-options" aria-label="Permalink to &quot;\`validateFlow(input, options?)\`&quot;">​</a></h3><p>Takes <code>unknown</code> on purpose: this is the boundary between untrusted JSON and the engine.</p><p><strong>It never throws, and it never repairs.</strong> Silently fixing a flow is how you ship a tour that is not the one the author drew — so a flow with errors comes back with <code>flow: null</code> and an issue list, never as a patched object.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">valid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">errors</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">warnings</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">flow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> validateFlow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">JSON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(text))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">valid) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> issue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> errors) console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(issue.path, issue.message, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;→&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, issue.hint)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gf.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(flow)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// narrowed to FlowDefinition</span></span></code></pre></div><h4 id="validateoptions" tabindex="-1"><code>ValidateOptions</code> <a class="header-anchor" href="#validateoptions" aria-label="Permalink to &quot;\`ValidateOptions\`&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>root</code></td><td><code>ParentNode</code></td><td>Enables the three selector rules. <strong>Omitted — the normal case in Node — they are skipped, never failed</strong></td></tr></tbody></table><p>That skip is what lets one function serve both callers: the same rules run in Node for <code>guideflow validate</code> and in a browser for a recorder, and a CI run never fails a flow just because Node has no DOM to resolve <code>#save-button</code> against.</p><h4 id="flowvalidation" tabindex="-1"><code>FlowValidation</code> <a class="header-anchor" href="#flowvalidation" aria-label="Permalink to &quot;\`FlowValidation\`&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>valid</code></td><td><code>boolean</code></td><td>True when <code>errors</code> is empty. Warnings never set it and never clear it</td></tr><tr><td><code>issues</code></td><td><code>FlowIssue[]</code></td><td>Every issue, in document order</td></tr><tr><td><code>errors</code></td><td><code>FlowIssue[]</code></td><td><code>severity === &#39;error&#39;</code></td></tr><tr><td><code>warnings</code></td><td><code>FlowIssue[]</code></td><td><code>severity === &#39;warning&#39;</code></td></tr><tr><td><code>flow</code></td><td><code>FlowDefinition | null</code></td><td>The flow, narrowed, when valid. <strong>Never a partially repaired flow</strong></td></tr></tbody></table><h4 id="flowissue" tabindex="-1"><code>FlowIssue</code> <a class="header-anchor" href="#flowissue" aria-label="Permalink to &quot;\`FlowIssue\`&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>code</code></td><td><code>FlowIssueCode</code></td><td>Stable and machine-readable. <strong>Assert on this</strong>; <code>message</code> may be reworded</td></tr><tr><td><code>severity</code></td><td><code>&#39;error&#39; | &#39;warning&#39;</code></td><td></td></tr><tr><td><code>path</code></td><td><code>string</code></td><td>Dotted, e.g. <code>states.welcome.steps[1].target</code>. Empty for flow-level issues</td></tr><tr><td><code>message</code></td><td><code>string</code></td><td>What is wrong</td></tr><tr><td><code>hint</code></td><td><code>string</code></td><td>One imperative sentence naming the fix. Rendered verbatim by the CLI</td></tr><tr><td><code>stateId</code></td><td><code>string</code></td><td>Present when the issue belongs to a state</td></tr><tr><td><code>stepId</code></td><td><code>string</code></td><td>Present when the issue belongs to a step</td></tr></tbody></table><h3 id="stringifyflowfile-flow-meta" tabindex="-1"><code>stringifyFlowFile(flow, meta?)</code> <a class="header-anchor" href="#stringifyflowfile-flow-meta" aria-label="Permalink to &quot;\`stringifyFlowFile(flow, meta?)\`&quot;">​</a></h3><p>The <strong>only</strong> writer. Returns the JSON text, pretty-printed, with a trailing newline.</p><p>It stamps <code>flow.version</code> via <code>withFingerprint</code> unless one is already set, so a returning user&#39;s resume point is invalidated when the flow&#39;s <em>shape</em> changes and not when a typo is fixed. See <a href="/GuideFlow/api/flow-definition">flow versioning</a>.</p><p><strong>It throws a <code>TypeError</code> when the flow contains anything JSON cannot hold</strong> — a function <code>showIf</code>, a <code>RegExp</code>, a <code>Date</code>. Refusing is the point: a file that silently dropped a <code>showIf</code> would mean something different from the flow it came from. Keep such flows in code.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { writeFileSync } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;node:fs&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { stringifyFlowFile } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@guideflow/core/authoring&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">writeFileSync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;welcome.flow.json&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stringifyFlowFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(flow, { generator: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;my-recorder&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }))</span></span></code></pre></div><h3 id="parseflowfile-input" tabindex="-1"><code>parseFlowFile(input)</code> <a class="header-anchor" href="#parseflowfile-input" aria-label="Permalink to &quot;\`parseFlowFile(input)\`&quot;">​</a></h3><p>The <strong>only</strong> reader. Tolerant about what it accepts and strict about what it returns. <strong>Never throws</strong> — malformed JSON comes back as a single <code>not-an-object</code> error, with <code>errors</code> populated so an exit-code check cannot pass a file that was not JSON at all.</p><p>Accepts three shapes:</p><ul><li>the <code>{ gfFlowFile: 1, flow, meta? }</code> envelope — <code>meta</code> is returned alongside the validation;</li><li>a bare <code>FlowDefinition</code>, for a file written by hand;</li><li>a legacy <code>{ kind: &#39;guideflow-draft&#39; }</code> editing session, which is run through <code>draftToFlow</code> first.</li></ul><p>A <code>string</code> input is <code>JSON.parse</code>d; anything else is used as-is. <code>meta</code> is <code>null</code> for every shape but the envelope.</p><h3 id="drafttoflow-draft-and-flowtodraft-flow" tabindex="-1"><code>draftToFlow(draft)</code> and <code>flowToDraft(flow)</code> <a class="header-anchor" href="#drafttoflow-draft-and-flowtodraft-flow" aria-label="Permalink to &quot;\`draftToFlow(draft)\` and \`flowToDraft(flow)\`&quot;">​</a></h3><p>The converter between the linear step list an editor produces and a real <code>FlowDefinition</code>.</p><p><code>draftToFlow</code> builds one state per step, chained on <code>NEXT</code>, with <code>final: true</code> on the last. Unlike <code>validateFlow</code>, it <strong>throws a <code>RangeError</code></strong> on an empty draft or a duplicate step id, rather than emitting a flow whose <code>goTo()</code> and resume would silently resolve to the wrong step.</p><p><code>flowToDraft</code> goes back the other way and is <strong>partial by design</strong>. It returns <code>draft: null</code> — never a quietly truncated list — for anything the linear model cannot hold, with <code>lossy</code> explaining what stopped it:</p><ul><li>a <code>NEXT</code> path that loops;</li><li>a state with <code>onEntry</code> / <code>onExit</code> hooks, or a <code>route</code>;</li><li>a state that branches on more than one event, or moves forward on an event other than <code>NEXT</code>;</li><li>a step whose <code>content</code>, <code>target</code> or <code>showIf</code> is a function.</li></ul><p><code>explainNotLinear(flow)</code> is the one-sentence form for a UI: the first <code>lossy</code> message, or <code>null</code> when the flow <em>is</em> linear.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> message</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> explainNotLinear</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(flow)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (message) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">showBanner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">message</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">} Edit this flow in code.\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h4 id="linearstep" tabindex="-1"><code>LinearStep</code> <a class="header-anchor" href="#linearstep" aria-label="Permalink to &quot;\`LinearStep\`&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>string</code></td><td></td></tr><tr><td><code>title</code></td><td><code>string</code></td><td>Becomes <code>content.title</code></td></tr><tr><td><code>body</code></td><td><code>string</code></td><td>Becomes <code>content.body</code></td></tr><tr><td><code>target</code></td><td><code>string | null</code></td><td><strong><code>null</code> means a centred modal announcement</strong>, which is a supported shape</td></tr><tr><td><code>placement</code></td><td><code>PopoverPlacement</code></td><td></td></tr><tr><td><code>padding</code></td><td><code>number</code></td><td></td></tr><tr><td><code>clickThrough</code></td><td><code>boolean</code></td><td></td></tr></tbody></table><p><code>FlowDraft</code> wraps them: <code>{ kind: &#39;guideflow-draft&#39;, draftVersion: 1, id, name, steps, sourceUrl? }</code>.</p><h2 id="the-rule-table" tabindex="-1">The rule table <a class="header-anchor" href="#the-rule-table" aria-label="Permalink to &quot;The rule table&quot;">​</a></h2><p>Thirty-two codes — seventeen errors and fifteen warnings. <code>FlowIssueCode</code> is a union, so a <code>switch</code> over it is exhaustive at compile time.</p><h3 id="errors" tabindex="-1">Errors <a class="header-anchor" href="#errors" aria-label="Permalink to &quot;Errors&quot;">​</a></h3><p>The engine crashes, refuses to run, or reports success it did not have.</p><table tabindex="0"><thead><tr><th>Code</th><th>What goes wrong</th></tr></thead><tbody><tr><td><code>not-an-object</code></td><td>Not a plain object — an array, a string, or unparseable JSON</td></tr><tr><td><code>unsupported-envelope</code></td><td><code>gfFlowFile</code> is present but is not <code>1</code>. Parsing stops</td></tr><tr><td><code>missing-id</code></td><td>No non-empty string <code>id</code>. Progress and completion are stored under it</td></tr><tr><td><code>missing-initial</code></td><td>No non-empty string <code>initial</code> naming the first state</td></tr><tr><td><code>no-states</code></td><td><code>states</code> is missing or empty. A flow is a state machine, not a step array</td></tr><tr><td><code>initial-not-found</code></td><td><code>initial</code> names a state that does not exist. <strong><code>FlowMachine</code> throws on this</strong></td></tr><tr><td><code>flat-steps-shape</code></td><td>Top-level <code>steps</code> and no <code>states</code> — a step list, not a flow. Convert it with <code>draftToFlow()</code></td></tr><tr><td><code>unknown-transition-target</code></td><td>A transition points at a state that does not exist. <strong>See below</strong></td></tr><tr><td><code>initial-state-has-no-steps</code></td><td>The tour starts active and paints nothing</td></tr><tr><td><code>duplicate-step-id</code></td><td><code>goTo()</code>, resume and the skip loop all resolve the first match</td></tr><tr><td><code>step-missing-id</code></td><td>A step is not an object, or has no non-empty string <code>id</code></td></tr><tr><td><code>step-missing-content</code></td><td>No <code>content.title</code>, <code>.body</code> or <code>.html</code> — the renderer has nothing to paint</td></tr><tr><td><code>unreachable-state</code></td><td>Not reachable from <code>initial</code>. Its steps ship but never run</td></tr><tr><td><code>route-on-step</code></td><td><code>route</code> belongs on a state. Nothing reads it on a step</td></tr><tr><td><code>invalid-target-type</code></td><td><code>target</code> is not a string or <code>null</code>. A function or an <code>Element</code> cannot survive being written to a file</td></tr><tr><td><code>invalid-selector</code></td><td><code>target</code> is not valid CSS, so the step can never resolve. Needs <code>options.root</code></td></tr></tbody></table><p><code>non-serialisable</code> is declared in <code>FlowIssueCode</code> but is not emitted by <code>validateFlow</code> — a flow holding a function or a <code>Date</code> is caught at write time, where <code>stringifyFlowFile</code> throws a <code>TypeError</code> instead.</p><h3 id="warnings" tabindex="-1">Warnings <a class="header-anchor" href="#warnings" aria-label="Permalink to &quot;Warnings&quot;">​</a></h3><p>It runs. It is probably not what you meant.</p><table tabindex="0"><thead><tr><th>Code</th><th>What goes wrong</th></tr></thead><tbody><tr><td><code>no-final-state</code></td><td>No state is marked <code>final: true</code>. <strong>See below</strong></td></tr><tr><td><code>forward-event-not-next</code></td><td>The single forward event is not named <code>NEXT</code>. <strong>See below</strong></td></tr><tr><td><code>off-default-path</code></td><td>A state with steps is reachable only by a non-<code>NEXT</code> event, so its steps are left out of the step count and the progress indicator under-reports</td></tr><tr><td><code>next-cycle</code></td><td>The <code>NEXT</code> path loops. <strong>See below</strong></td></tr><tr><td><code>state-has-no-steps</code></td><td>Fine as a routing-only or terminal marker state; otherwise you forgot one</td></tr><tr><td><code>final-state-with-outgoing</code></td><td><code>final: true</code> next to an <code>on</code> table — the two contradict each other</td></tr><tr><td><code>invalid-placement</code></td><td>Not one of the twelve placements plus <code>center</code>; the popover falls back silently</td></tr><tr><td><code>missing-target</code></td><td>No <code>target</code> and no <code>placement: &#39;center&#39;</code>, so it renders as a centred modal by accident</td></tr><tr><td><code>fragile-selector</code></td><td><code>target</code> looks like a framework-generated id, which changes on the next render. Pure string analysis, so <strong>this one runs in Node</strong></td></tr><tr><td><code>selector-not-unique</code></td><td>Matches more than one element; the tour highlights the first. Needs <code>options.root</code></td></tr><tr><td><code>selector-no-match</code></td><td>Matches nothing on this page. The step waits, then renders unanchored. Needs <code>options.root</code></td></tr><tr><td><code>targeting-without-subpath</code></td><td><code>targeting</code> is inert — core reads none of it. Install <code>createTargeting(gf)</code> from <code>@guideflow/core/targeting</code></td></tr><tr><td><code>route-without-navigation</code></td><td><code>route</code> is inert without <code>navigation: createNavigation()</code> from <code>@guideflow/core/navigation</code></td></tr><tr><td><code>html-content-without-sanitizer</code></td><td><code>content.html</code> is escaped and rendered as text unless <code>createGuideFlow({ sanitizeHTML })</code> is passed from <code>@guideflow/core/html</code></td></tr><tr><td><code>too-many-steps</code></td><td>More than 25 steps in one tour. Consider splitting it, or a <a href="/GuideFlow/guide/checklist">checklist</a> for the long tail</td></tr></tbody></table><h3 id="measured-not-assumed" tabindex="-1">Measured, not assumed <a class="header-anchor" href="#measured-not-assumed" aria-label="Permalink to &quot;Measured, not assumed&quot;">​</a></h3><p>Four severities look surprising until you see what the engine actually does. Each was run against the real engine, not reasoned about:</p><table tabindex="0"><thead><tr><th>Shape</th><th>Measured behaviour</th><th>Severity</th></tr></thead><tbody><tr><td>A transition naming a state that does not exist</td><td>One <code>console.warn</code>, then <strong><code>tour:complete</code></strong> — the tour truncates <em>and</em> is marked completed, so it never shows the user again</td><td><strong>error</strong></td></tr><tr><td>No <code>final: true</code> state anywhere</td><td>The tour <strong>completes normally</strong>; <code>tour:complete</code> fires when it runs out of steps</td><td>warning</td></tr><tr><td>A forward event not named <code>NEXT</code></td><td><code>totalSteps</code> reports <strong>1</strong> when the true total is <strong>2</strong>, so the renderer puts &quot;Done&quot; on step one</td><td>warning</td></tr><tr><td>A <code>NEXT</code> cycle</td><td><code>totalSteps</code> is arbitrary. No hang</td><td>warning</td></tr></tbody></table><p>The unknown-transition-target case is the worst shape in the set, and the reason this module exists: it fails <em>as success</em>. Nothing surfaces to the user, and the flow is written off as completed, so the tour is gone for good.</p><p>The no-final-state case corrects a long-standing claim in this repo&#39;s own engineering notes, which said for eight phases that such a flow &quot;never completes&quot;. Measurement says otherwise. Marking the last state <code>final: true</code> is still worth doing — it says so explicitly and it fixes the step counter — which is exactly what a warning is for.</p><h3 id="in-ci" tabindex="-1">In CI <a class="header-anchor" href="#in-ci" aria-label="Permalink to &quot;In CI&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">guideflow</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> validate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flows/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">*</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.flow.json</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">guideflow</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> validate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flows/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">*</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.flow.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --strict</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # warnings fail too</span></span></code></pre></div><p>Exit <code>0</code> means no errors. Exit <code>1</code> means at least one error, an unreadable file, or — with <code>--strict</code> — any warning. Each issue prints its <code>message</code>, then its <code>hint</code> on a <code>→</code> line. See the <a href="/GuideFlow/api/cli">CLI reference</a>.</p><h2 id="size" tabindex="-1">Size <a class="header-anchor" href="#size" aria-label="Permalink to &quot;Size&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Bundle</th><th>Size</th><th>Gate</th></tr></thead><tbody><tr><td><code>@guideflow/core/authoring</code></td><td><strong>5.35 kB</strong> gzip</td><td>5.5 kB</td></tr><tr><td><code>@guideflow/core/selector</code></td><td><strong>1.76 kB</strong> gzip</td><td>2.5 kB</td></tr><tr><td><code>@guideflow/core</code> (entry)</td><td>15.13 kB gzip</td><td>15.5 kB — <strong>untouched by these two</strong></td></tr></tbody></table><p><code>authoring</code> is large next to <code>./targeting</code>&#39;s 2.18 kB, and the difference is rules, not prose: stripping every <code>message</code> and <code>hint</code> in the file saves 880 B. The hints are the deliverable — they are what turns &quot;this flow is invalid&quot; into something a non-engineer can act on — so the gate was set from a measurement rather than an estimate.</p><p><strong>Neither subpath is imported by <code>src/index.ts</code></strong>, so neither can reach an application bundle unless you import it yourself. Do not: validation is an authoring-time and CI concern. Your users never need to know a flow was valid, because it was checked before it shipped.</p><h2 id="guideflow-core-selector" tabindex="-1"><code>@guideflow/core/selector</code> <a class="header-anchor" href="#guideflow-core-selector" aria-label="Permalink to &quot;\`@guideflow/core/selector\`&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { buildSelector } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@guideflow/core/selector&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">selector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">confidence</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unique</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">warnings</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> buildSelector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(el)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">unique) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> refuseToSaveTheStep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(warnings)</span></span></code></pre></div><p>Building a CSS selector for an element is the make-or-break of any recorder: a selector that silently resolves to the wrong element ships a tour that highlights the wrong thing, and nobody finds out until a user does.</p><table tabindex="0"><thead><tr><th>Export</th><th>Signature</th></tr></thead><tbody><tr><td><code>buildSelector</code></td><td><code>(el: Element, options?: SelectorOptions) =&gt; SelectorResult</code></td></tr><tr><td><code>verifySelector</code></td><td><code>(selector: string, el: Element, root?: ParentNode) =&gt; &#39;unique&#39; | &#39;ambiguous&#39; | &#39;no-match&#39; | &#39;invalid&#39;</code></td></tr><tr><td><code>isStableId</code></td><td><code>(id: string) =&gt; boolean</code></td></tr><tr><td><code>retargetToInteractive</code></td><td><code>(el: Element, maxClimb?: number) =&gt; Element</code></td></tr></tbody></table><p>Types: <code>SelectorResult</code>, <code>SelectorStrategy</code>, <code>SelectorConfidence</code>, <code>SelectorWarning</code>, <code>SelectorOptions</code>.</p><p>The module is purely DOM-computational — it reads elements and attributes and re-queries the document it was given. No layout, no geometry, no <code>window</code>.</p><h3 id="every-candidate-is-verified-by-re-query" tabindex="-1">Every candidate is verified by re-query <a class="header-anchor" href="#every-candidate-is-verified-by-re-query" aria-label="Permalink to &quot;Every candidate is verified by re-query&quot;">​</a></h3><p><code>unique</code> is <strong>verified, not asserted</strong>: the selector is run back through <code>root.querySelectorAll()</code> and checked for exactly one match that is the element in question.</p><p><code>unique: false</code> means nothing resolved. <strong>The caller must refuse the step</strong> rather than ship a selector that points somewhere else. There is always a <code>selector</code> string — it is never empty — but an unverified one is a guess, and a recorder that saves it has produced a broken tour that looks fine.</p><h3 id="selectorresult" tabindex="-1"><code>SelectorResult</code> <a class="header-anchor" href="#selectorresult" aria-label="Permalink to &quot;\`SelectorResult\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>selector</code></td><td><code>string</code></td><td>Never empty</td></tr><tr><td><code>strategy</code></td><td><code>SelectorStrategy</code></td><td>Which rule produced it</td></tr><tr><td><code>confidence</code></td><td><code>SelectorConfidence</code></td><td>How well it should survive a deploy</td></tr><tr><td><code>unique</code></td><td><code>boolean</code></td><td>Verified by re-query</td></tr><tr><td><code>matchCount</code></td><td><code>number</code></td><td></td></tr><tr><td><code>element</code></td><td><code>Element</code></td><td><strong>What the selector actually resolves to.</strong> Differs from the input after retargeting</td></tr><tr><td><code>warnings</code></td><td><code>SelectorWarning[]</code></td><td></td></tr></tbody></table><h3 id="strategies" tabindex="-1">Strategies <a class="header-anchor" href="#strategies" aria-label="Permalink to &quot;Strategies&quot;">​</a></h3><p>Tried in this order. The first one that verifies wins.</p><table tabindex="0"><thead><tr><th>#</th><th>Strategy</th><th>Shape</th><th>Confidence</th></tr></thead><tbody><tr><td>1</td><td><code>gf-id</code></td><td><code>[data-gf-id=&quot;save&quot;]</code></td><td><code>stable</code></td></tr><tr><td>2</td><td><code>testid</code></td><td><code>[data-testid=&quot;save&quot;]</code></td><td><code>stable</code></td></tr><tr><td>3</td><td><code>id</code></td><td><code>#save-button</code></td><td><code>stable</code></td></tr><tr><td>4</td><td><code>name</code></td><td><code>input[name=&quot;email&quot;]</code></td><td><code>stable</code></td></tr><tr><td>5</td><td><code>aria-label</code></td><td><code>button[aria-label=&quot;Close&quot;]</code></td><td><code>semantic</code></td></tr><tr><td>6</td><td><code>href</code></td><td><code>a[href=&quot;/settings&quot;]</code></td><td><code>semantic</code></td></tr><tr><td>7</td><td><code>structural</code></td><td><code>main &gt; section:nth-of-type(2) &gt; button:nth-of-type(1)</code></td><td><code>fragile</code></td></tr></tbody></table><p>Notes on the non-obvious ones:</p><ul><li><strong><code>data-gf-id</code> is a documented opt-in hook, and it wins outright.</strong> Add it to the elements you want tours to point at. It is the one strategy where the author has said, in the markup, &quot;this is a tour anchor&quot; — nothing else in this list is a promise, it is an inference. If you own the app you are touring, this is the cheapest durability you can buy.</li><li><strong>Test ids rank above <code>aria-label</code>.</strong> An <code>aria-label</code> is user-visible text: it collides across a page and it changes when the page is translated, which is why that strategy always carries an <code>i18n-fragile</code> warning.</li><li><strong><code>id</code> is used only when <a href="#isstableid-id"><code>isStableId</code></a> accepts it.</strong> A rejected id does not disqualify the element; it demotes to the next strategy and adds a <code>generated-id</code> warning.</li><li><strong><code>name</code> applies to <code>input</code>, <code>select</code> and <code>textarea</code> only</strong>, and is tag-scoped. It is submitted to a server, so it is as stable as the backend contract.</li><li><strong><code>href</code> applies to <code>&lt;a&gt;</code> only</strong>, and only when the value is at most 60 characters and has no query or fragment.</li><li><strong>The structural path is anchored</strong>, not free-floating: the walk climbs until the path either resolves uniquely or is rooted on an ancestor carrying a stable anchor. It emits <code>:nth-of-type</code>, not <code>:nth-child</code>, because a portal or tooltip <code>&lt;div&gt;</code> appended as a sibling shifts every <code>nth-child</code> index on the page. Case is preserved outside the HTML namespace, so an SVG <code>clipPath</code> is never lowercased into <code>clippath</code>, which matches nothing.</li></ul><h3 id="warnings-1" tabindex="-1">Warnings <a class="header-anchor" href="#warnings-1" aria-label="Permalink to &quot;Warnings&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Warning</th><th>Meaning</th></tr></thead><tbody><tr><td><code>generated-id</code></td><td>An id was present and was rejected as framework-generated</td></tr><tr><td><code>i18n-fragile</code></td><td>The selector embeds user-visible text, so translating the page breaks it</td></tr><tr><td><code>positional</code></td><td>The path contains <code>:nth-of-type</code>, so re-ordering the markup breaks it</td></tr><tr><td><code>not-unique</code></td><td>Nothing resolved uniquely. <code>unique</code> is <code>false</code> and the result is a best effort</td></tr><tr><td><code>redacted</code></td><td>Inside <code>[data-gf-private]</code> — only structural segments were used</td></tr><tr><td><code>retargeted</code></td><td>The selector points at an interactive ancestor, not the element passed in</td></tr><tr><td><code>shadow-dom</code></td><td>Inside a shadow root: no document-level selector can reach it</td></tr></tbody></table><p><code>shadow-dom</code> always comes with <code>unique: false</code>, because <code>TourEngine</code> resolves targets with <code>document.querySelector</code>, which cannot cross a shadow boundary. Saying so is more useful than returning a selector that cannot work.</p><p><code>redacted</code> is evaluated once, at entry, before any strategy runs — so ids and test ids cannot walk out of an opted-out subtree.</p><h3 id="selectoroptions" tabindex="-1"><code>SelectorOptions</code> <a class="header-anchor" href="#selectoroptions" aria-label="Permalink to &quot;\`SelectorOptions\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>testIdAttributes</code></td><td><code>readonly string[]</code></td><td><code>data-testid</code>, <code>data-test-id</code>, <code>data-test</code>, <code>data-cy</code>, <code>data-qa</code>, <code>data-pw</code></td><td>Tried in order; the first present wins</td></tr><tr><td><code>privateSelector</code></td><td><code>string</code></td><td><code>&#39;[data-gf-private]&#39;</code></td><td>Subtree opt-out marker</td></tr><tr><td><code>root</code></td><td><code>ParentNode</code></td><td><code>el.ownerDocument</code></td><td>Where uniqueness is verified</td></tr><tr><td><code>maxDepth</code></td><td><code>number</code></td><td><code>12</code></td><td>Ancestor segments before the walk gives up</td></tr><tr><td><code>retargetDepth</code></td><td><code>number</code></td><td><code>4</code></td><td>Levels climbed looking for an interactive ancestor. <code>0</code> disables</td></tr></tbody></table><h3 id="isstableid-id" tabindex="-1"><code>isStableId(id)</code> <a class="header-anchor" href="#isstableid-id" aria-label="Permalink to &quot;\`isStableId(id)\`&quot;">​</a></h3><p>Is this id worth building a selector out of? Rejects React 18&#39;s <code>:r1:</code> and React 19&#39;s <code>«r1»</code>, component-library prefixes (<code>radix</code>, <code>headlessui</code>, <code>mui</code>, <code>mantine</code>, <code>chakra</code>, <code>reach</code>, <code>downshift</code>, <code>rc</code>, <code>ember</code>, <code>aria</code>), UUIDs, bare hex and content hashes, nanoid-shaped strings, anything longer than 64 characters, and anything containing a run of four or more digits — timestamps, row counters and database keys.</p><p><strong>The asymmetry is deliberate.</strong> A false reject costs one rank position and an amber badge. A false accept ships <code>#:r1:</code> — syntactically valid, unique today, and matching nothing after the next render. Bias toward rejection.</p><h3 id="retargettointeractive-el-maxclimb" tabindex="-1"><code>retargetToInteractive(el, maxClimb?)</code> <a class="header-anchor" href="#retargettointeractive-el-maxclimb" aria-label="Permalink to &quot;\`retargetToInteractive(el, maxClimb?)\`&quot;">​</a></h3><p>Climbs to the interactive ancestor a user meant to click, so that clicking the <code>&lt;path&gt;</code> inside <code>&lt;button&gt;&lt;svg&gt;&lt;path/&gt;&lt;/svg&gt;&lt;/button&gt;</code> records the button. An element inside an <code>&lt;svg&gt;</code> is never a tour target, so the climb jumps straight out of the SVG.</p><p>Recognised as interactive: <code>a[href]</code>, <code>button</code>, <code>input</code>, <code>select</code>, <code>textarea</code>, <code>summary</code>, <code>label</code>, the <code>button</code> / <code>link</code> / <code>tab</code> / <code>menuitem</code> / <code>checkbox</code> / <code>radio</code> / <code>switch</code> / <code>option</code> roles, <code>[tabindex]</code> other than <code>-1</code>, and <code>[contenteditable=&quot;true&quot;]</code>.</p><h3 id="verifyselector-selector-el-root" tabindex="-1"><code>verifySelector(selector, el, root?)</code> <a class="header-anchor" href="#verifyselector-selector-el-root" aria-label="Permalink to &quot;\`verifySelector(selector, el, root?)\`&quot;">​</a></h3><p>Does <code>selector</code> resolve to exactly <code>el</code>, and nothing else? <strong>Never throws</strong> — a malformed candidate returns <code>&#39;invalid&#39;</code> so it can demote to the next strategy rather than propagate out of a recorder.</p><p>The identity check is not optional: after retargeting, a selector can match exactly one element and still not be the one being described, which is why a single match that is not <code>el</code> returns <code>&#39;no-match&#39;</code> rather than <code>&#39;unique&#39;</code>.</p><h3 id="verified-against-real-chromium" tabindex="-1">Verified against real Chromium <a class="header-anchor" href="#verified-against-real-chromium" aria-label="Permalink to &quot;Verified against real Chromium&quot;">​</a></h3><p>The previous three implementations of this logic — one each in <code>@guideflow/ai</code>, <code>@guideflow/devtools</code> and the recorder — were broken in the same two ways: they trusted framework-generated ids, and <strong>none of them ever re-queried to check the selector they had just built</strong>. All three now call this module. Old versus new, against the built artifact:</p><table tabindex="0"><thead><tr><th>Page shape</th><th>Old</th><th>New</th></tr></thead><tbody><tr><td>Two buttons sharing <code>aria-label=&quot;Close&quot;</code></td><td>Matched 2, <strong>pointed at the wrong one</strong></td><td><code>#banner &gt; button:nth-of-type(1)</code>, 1 match</td></tr><tr><td>Sidebar and main with matching nesting</td><td>Matched 2, <strong>pointed at the sidebar</strong></td><td>Anchored at <code>main</code>, 1 match</td></tr><tr><td>An icon inside a button</td><td>Anchored the inner <code>&lt;path&gt;</code></td><td>Retargets to the button, <code>[data-testid=&quot;save&quot;]</code></td></tr><tr><td>A React <code>useId</code> value, <code>#:r1:</code></td><td>Emitted it — dead on the next render</td><td>Rejects it, warns <code>generated-id</code>, anchors elsewhere</td></tr></tbody></table><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/GuideFlow/api/flow-definition">FlowDefinition</a> — the type inside the envelope</li><li><a href="/GuideFlow/guide/flows-and-steps">Flows &amp; Steps</a> — how a flow is put together</li><li><a href="/GuideFlow/guide/state-machine">State Machine</a> — transitions, <code>final</code>, and the <code>NEXT</code> path</li><li><a href="/GuideFlow/api/cli">CLI reference</a> — <code>guideflow validate</code> and <code>guideflow export</code></li></ul>`,108)])])}const k=t(o,[["render",d]]);export{g as __pageData,k as default};
