import{_ as t,o,c as s,ag as i}from"./chunks/framework.CVaJrGn7.js";const u=JSON.parse('{"title":"Accessibility","description":"What GuideFlow does for screen-reader and keyboard users out of the box, what it deliberately does not do, and what you still have to do yourself.","frontmatter":{"description":"What GuideFlow does for screen-reader and keyboard users out of the box, what it deliberately does not do, and what you still have to do yourself.","keywords":"product tour accessibility, WCAG tour library, screen reader onboarding, keyboard accessible tour, prefers-reduced-motion","head":[["link",{"rel":"canonical","href":"https://realnerdzw.github.io/GuideFlow/guide/accessibility"}],["meta",{"property":"og:url","content":"https://realnerdzw.github.io/GuideFlow/guide/accessibility"}],["meta",{"property":"og:title","content":"Accessibility — GuideFlow.js"}],["meta",{"property":"og:description","content":"What GuideFlow does for screen-reader and keyboard users out of the box, what it deliberately does not do, and what you still have to do yourself."}],["meta",{"name":"twitter:title","content":"Accessibility — GuideFlow.js"}],["meta",{"name":"twitter:description","content":"What GuideFlow does for screen-reader and keyboard users out of the box, what it deliberately does not do, and what you still have to do yourself."}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"BreadcrumbList\\",\\"itemListElement\\":[{\\"@type\\":\\"ListItem\\",\\"position\\":1,\\"name\\":\\"Home\\",\\"item\\":\\"https://realnerdzw.github.io/GuideFlow/\\"},{\\"@type\\":\\"ListItem\\",\\"position\\":2,\\"name\\":\\"Guide\\",\\"item\\":\\"https://realnerdzw.github.io/GuideFlow/guide/\\"},{\\"@type\\":\\"ListItem\\",\\"position\\":3,\\"name\\":\\"Accessibility\\",\\"item\\":\\"https://realnerdzw.github.io/GuideFlow/guide/accessibility/\\"}]}"]]},"headers":[],"relativePath":"guide/accessibility.md","filePath":"guide/accessibility.md","lastUpdated":1786207781000}'),a={name:"guide/accessibility.md"};function n(r,e,d,l,h,c){return o(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="accessibility" tabindex="-1">Accessibility <a class="header-anchor" href="#accessibility" aria-label="Permalink to &quot;Accessibility&quot;">​</a></h1><p>A product tour sits on top of someone else&#39;s application and takes over the screen. That makes it one of the easiest things in a codebase to get wrong for keyboard and screen-reader users — and one of the most damaging, because it is the <em>first</em> thing a new user meets.</p><p>This page describes what the library does, what it deliberately does not do, and what is still on you.</p><div class="warning custom-block"><p class="custom-block-title">Verified by tooling, not yet by a person</p><p>Everything below is covered by automated tests — axe reports no critical or serious violations on an open popover, and the focus, keyboard and announcement behaviour is asserted in a real browser across Chromium, Firefox and WebKit.</p><p><strong>No manual screen-reader pass has been run.</strong> Automated checks catch structure, not usability. If accessibility is a requirement you have to sign off on, test with your own users and your own assistive technology before you rely on this.</p></div><h2 id="keyboard" tabindex="-1">Keyboard <a class="header-anchor" href="#keyboard" aria-label="Permalink to &quot;Keyboard&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Key</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>Tab</code> / <code>Shift+Tab</code></td><td>Cycles the popover&#39;s own controls. Focus cannot leave the dialog while a step is open.</td></tr><tr><td><code>→</code> / <code>↓</code></td><td>Next step</td></tr><tr><td><code>←</code> / <code>↑</code></td><td>Previous step</td></tr><tr><td><code>Escape</code></td><td>Ends the tour</td></tr></tbody></table><p>Focus moves into the popover when a step opens and returns to whatever held it before the tour started when the tour ends — the button the user clicked to start it, usually.</p><h3 id="arrow-keys-and-your-inputs" tabindex="-1">Arrow keys and your inputs <a class="header-anchor" href="#arrow-keys-and-your-inputs" aria-label="Permalink to &quot;Arrow keys and your inputs&quot;">​</a></h3><p>The arrow-key bindings live on <code>document</code>, so they could easily steal keystrokes from your app. They do not fire when the event targets:</p><ul><li>an <code>&lt;input&gt;</code>, <code>&lt;textarea&gt;</code> or <code>&lt;select&gt;</code></li><li>anything inside <code>[contenteditable]</code></li><li>an element with a widget <code>role</code> (<code>textbox</code>, <code>combobox</code>, <code>listbox</code>, <code>slider</code>, <code>menu</code>, <code>grid</code>, …)</li><li>a key that is part of an IME composition</li><li>a key held with <code>Ctrl</code>, <code>Alt</code> or <code>Meta</code></li><li>a key another handler has already called <code>preventDefault()</code> on</li></ul><p>This matters most for <a href="/GuideFlow/guide/spotlight-popover#click-through"><code>clickThrough</code></a> steps, which exist precisely so the user can type into the highlighted element.</p><p><code>Escape</code> is the exception: it fires from anywhere, including from inside a text field, because it is a keyboard user&#39;s only guaranteed way out of a modal.</p><h2 id="screen-readers" tabindex="-1">Screen readers <a class="header-anchor" href="#screen-readers" aria-label="Permalink to &quot;Screen readers&quot;">​</a></h2><p>The popover is a <code>role=&quot;dialog&quot;</code> with <code>aria-modal=&quot;true&quot;</code>. It takes its accessible name from the step title, and falls back to a translatable <code>dialogLabel</code> string when a step has no title. The body is wired up with <code>aria-describedby</code>, and both references are removed rather than left dangling when the corresponding content is absent.</p><p>Each step is announced through a polite live region. This is necessary because the popover element is <strong>reused</strong> between steps: a screen reader sees no new node, and moving focus reads only the focused button, not the step body. The region lives outside the popover so it survives the popover being removed.</p><p>The progress bar reports a step count, not a percentage:</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">div</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> role</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;progressbar&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">     aria-label</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Tour progress&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">     aria-valuenow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> aria-valuemin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> aria-valuemax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;4&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">     aria-valuetext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Step 2 of 4&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>All four strings — <code>dialogLabel</code>, <code>progressLabel</code>, <code>stepOf</code>, and the button labels — go through the <a href="/GuideFlow/guide/i18n">i18n registry</a>, so they are announced in the user&#39;s language:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gf.i18n.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fr&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  dialogLabel: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Visite guidée&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  progressLabel: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Progression&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  stepOf: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Étape {current} sur {total}&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gf.i18n.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">use</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fr&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="reduced-motion" tabindex="-1">Reduced motion <a class="header-anchor" href="#reduced-motion" aria-label="Permalink to &quot;Reduced motion&quot;">​</a></h2><p>Under <code>prefers-reduced-motion: reduce</code> the library stops the popover entrance animation, the progress-bar and button transitions, the spotlight cutout&#39;s slide between targets, the hotspot beacon&#39;s pulse, and the smooth page scroll — the last two being the ones most likely to cause trouble, since one animates forever and the other moves the whole viewport.</p><p>Two of those are set from JavaScript and cannot be reached by a stylesheet, so this works whether or not you import <code>@guideflow/core/styles</code>.</p><h2 id="forced-colors-high-contrast" tabindex="-1">Forced colors / High Contrast <a class="header-anchor" href="#forced-colors-high-contrast" aria-label="Permalink to &quot;Forced colors / High Contrast&quot;">​</a></h2><p>Under <code>forced-colors: active</code> the popover, buttons and progress bar declare system colour keywords and let the OS map them. Opacity-based de-emphasis is reset, because opacity is not a colour and survives the forced palette. The spotlight cutout gains an outline, since the dimmed overlay itself disappears.</p><p>There is also an opt-in high-contrast theme independent of any OS setting:</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">html</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> data-gf-high-contrast</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><h2 id="right-to-left" tabindex="-1">Right-to-left <a class="header-anchor" href="#right-to-left" aria-label="Permalink to &quot;Right-to-left&quot;">​</a></h2><p>Set <code>dir=&quot;rtl&quot;</code> on your document and the popover follows. There is almost nothing RTL-specific in the stylesheet, and that is deliberate — flexbox and block layout already mirror themselves, so hand-written <code>[dir=&quot;rtl&quot;]</code> rules usually undo the browser&#39;s correct behaviour rather than fixing it.</p><p>Placement follows the CSS logical-property convention:</p><ul><li><code>top-start</code> / <code>bottom-start</code> align to the edge text <strong>begins</strong> at — the right edge in RTL</li><li><code>left</code> and <code>right</code> stay <strong>physical</strong>, so <code>placement: &#39;left&#39;</code> means left for everyone</li></ul><h2 id="contrast" tabindex="-1">Contrast <a class="header-anchor" href="#contrast" aria-label="Permalink to &quot;Contrast&quot;">​</a></h2><p>The default palette meets WCAG 2.1 AA. If you override the tokens, two constraints carry:</p><table tabindex="0"><thead><tr><th>Token</th><th>Constraint</th></tr></thead><tbody><tr><td><code>--gf-accent-color</code></td><td>≥ 4.5:1 against <code>--gf-accent-fg</code> — it is the primary button&#39;s background</td></tr><tr><td><code>--gf-muted-opacity</code></td><td>Raise it if you darken <code>--gf-popover-bg</code>; it de-emphasises real text</td></tr></tbody></table><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">:root</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --gf-accent-color</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">#0f766e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --gf-accent-fg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">#ffffff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="what-you-still-have-to-do" tabindex="-1">What you still have to do <a class="header-anchor" href="#what-you-still-have-to-do" aria-label="Permalink to &quot;What you still have to do&quot;">​</a></h2><p><strong>Give every target an accessible name.</strong> The tour points at your elements; it cannot describe them. A step highlighting an icon-only button announces the popover&#39;s text and nothing about what is being highlighted.</p><p><strong>Do not rely on the spotlight alone to convey meaning.</strong> It is a visual affordance. The step body has to say what the user is looking at.</p><p><strong>Write step text that stands on its own.</strong> &quot;Click here&quot; is meaningless to someone who cannot see where &quot;here&quot; is. Name the control.</p><p><strong><code>clickThrough</code> steps are keyboard-reachable.</strong> The focus trap widens to include the highlighted element, so <code>Tab</code> reaches it and <code>Shift</code>+<code>Tab</code> returns — the same hole the <code>clip-path</code> cuts for the mouse, cut in the tab order — and <code>aria-modal</code> is dropped, because on those steps the page provably is not inert. The widening is exactly one element: everything else stays trapped.</p><p>Still worth testing by keyboard, for one reason the library cannot fix for you. <code>Enter</code> on a native <code>&lt;button&gt;</code> or <code>&lt;a href&gt;</code> synthesises a click; a <code>&lt;div role=&quot;button&quot; tabindex=&quot;0&quot;&gt;</code> with an <code>onKeyDown</code> handler does not, so an <code>advanceOn</code> click rule will not notice it. Give those steps an app-dispatched <code>CustomEvent</code> instead — see <a href="./advance-on">Advancing on interaction</a>.</p><p><strong>The overlay is not <code>inert</code>.</strong> Background content stays focusable and in the accessibility tree. This is a deliberate trade: making it inert would break <code>clickThrough</code> entirely. The focus trap is what keeps <code>Tab</code> out of it — belt to that brace. If your tour never uses <code>clickThrough</code> and you want stricter isolation, apply <code>inert</code> to your app root yourself while a tour is running.</p><h2 id="the-checklist-widget" tabindex="-1">The checklist widget <a class="header-anchor" href="#the-checklist-widget" aria-label="Permalink to &quot;The checklist widget&quot;">​</a></h2><p><code>@guideflow/checklist</code> ships a docked UI, and a shipped surface absent from this page is an undocumented accessibility claim. What it does:</p><ul><li><strong>A disclosure, not a dialog.</strong> No <code>role=&quot;dialog&quot;</code>, no <code>aria-modal</code>, and <strong>no focus trap</strong> — a persistent docked surface that swallowed <code>Tab</code> would be a keyboard trap under WCAG 2.1.2, and a second capture-phase trap competing with the renderer&#39;s would deadlock the keyboard outright. An e2e spec asserts focus can leave the widget in both directions.</li><li><strong>Hidden and <code>inert</code> during a tour.</strong> <code>visibility: hidden</code> removes the subtree from the tab order <em>and</em> the accessibility tree; <code>inert</code> is set alongside it, because on a browser without <code>inert</code> focus would otherwise land on an invisible widget. Its z-index sits below <code>--gf-z-overlay</code>, so the overlay dims and covers it.</li><li><strong>Its own polite live region</strong>, outside the panel, announcing the aggregate only — &quot;Invite your team, complete. 3 of 5 complete.&quot; — never per-item chatter. Announcements are held while a tour is up and flushed afterwards, so the two regions never talk over each other.</li><li><strong>Blocked rows are <code>aria-disabled=&quot;true&quot;</code>, never <code>disabled</code></strong>, so they stay focusable and can describe which item unblocks them — by title, not by internal id.</li><li><strong>Progress is a count, not a percentage</strong>: <code>role=&quot;progressbar&quot;</code> with <code>aria-valuetext</code> reading &quot;3 of 5 complete&quot;.</li><li><strong>Done is a glyph plus visually-hidden text</strong>, never colour alone.</li><li><code>prefers-reduced-motion</code> and <code>forced-colors</code> blocks ship inside the widget&#39;s own stylesheet, so they apply even to consumers who never import <code>@guideflow/core/styles</code>. Every de-emphasis is an opacity, which is not a colour and therefore survives a forced palette — each one is reset to <code>1</code> explicitly. <code>forced-color-adjust: none</code> is deliberately <strong>not</strong> used.</li><li><strong>RTL is the browser&#39;s job.</strong> <code>inset-inline-end</code> plus flexbox; zero <code>[dir=&quot;rtl&quot;]</code> rules and zero JS mirroring.</li><li>Touch targets: launcher ≥ 44 × 44, rows ≥ 44 px tall, every control ≥ 24 × 24.</li></ul><p>What is <strong>not</strong> verified: <code>forced-colors: active</code> is not emulable in Playwright, so that block is reviewed rather than tested — and the warning at the top of this page applies here too. No manual screen-reader pass has been run on the checklist either.</p><h2 id="custom-renderers" tabindex="-1">Custom renderers <a class="header-anchor" href="#custom-renderers" aria-label="Permalink to &quot;Custom renderers&quot;">​</a></h2><p>If you implement your own <a href="/GuideFlow/guide/spotlight-popover#custom-renderer"><code>RendererContract</code></a>, none of the above comes with it — the focus trap, live region and ARIA wiring all live in the default renderer. At minimum, reproduce:</p><ul><li>a focus trap and focus restoration around your dialog</li><li>a polite live region announcing each step, outside the element you reuse between steps</li><li>an accessible name on the dialog, and <code>aria-describedby</code> for the body</li><li><code>setI18n(registry)</code>, so your strings follow <code>gf.i18n.use(locale)</code></li></ul><p><code>@guideflow/react</code>&#39;s <code>GuidePopover</code> is a working reference for all four.</p>`,49)])])}const g=t(a,[["render",n]]);export{u as __pageData,g as default};
