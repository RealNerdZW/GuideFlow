/**
 * Extension version, substituted at build time by Vite's `define` from the
 * `version` field of `packages/devtools/package.json` — the single source of
 * truth. The same value is written into `dist/manifest.json`.
 *
 * See the `copy-static` plugin in `packages/devtools/vite.config.ts`.
 */
declare const __GF_VERSION__: string;
