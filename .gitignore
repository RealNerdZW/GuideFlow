# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
.next
out
.svelte-kit
# Storybook build output — untracked 2026-07-30 (was 29 committed files, ~7.8 MB,
# rewritten by every `storybook build` and so permanently dirtying the tree).
storybook-static

# Coverage
coverage
*.lcov

# Cache
.turbo
.cache
.parcel-cache
.eslintcache
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.idea
*.swp
*.swo
.vscode/settings.json

# Claude — commit shared assets (docs/skills/agents/commands/settings.json),
# ignore only per-developer local state.
.claude/settings.local.json
.claude/local/
.claude/*.local.*

# Test results
test-results
playwright-report
**/e2e/screenshots

# Extension builds
packages/devtools/web-ext-artifacts
*.crx
*.xpi
